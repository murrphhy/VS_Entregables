
Vagrant.configure("2") do |config|
  config.vm.define "kubernetes" do |vm1|
    vm1.vm.box = "ubuntu/focal64"
    vm1.vm.hostname = "kubernetes"
    vm1.vm.provision :shell, path: "provisioning/kubernetes.sh"
 end
end
